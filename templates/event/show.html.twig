{% extends 'base.html.twig' %}

{% block title %}Event{% endblock %}

{% block body %}
    <h1>Event</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ event.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_index', {'rule': event.rule.id}) }}">back to list</a>

    <a href="{{ path('event_edit', {'id': event.id, 'rule': event.rule.id}) }}">edit</a>

    {{ include('event/_delete_form.html.twig') }}
{% endblock %}
